import styles from './share.module.css';
import { Title } from '../../common/Title/Title';

export const Share = () => {
    return (
        <div className={styles.share}>
            <Title title='Акции' />
        </div>
    )

}